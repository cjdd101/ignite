# 修复记录 #4

## 问题信息
- **发现周期**: 第1周期
- **发现轮次**: 第2轮
- **发现时间**: 2026-04-27
- **审计维度**: 模块串联校验
- **主题**: Flame类型与代码不一致

## 问题描述
Flame.status 类型定义是 `'burning' | 'burned'`，但代码中使用 `'active'`。同时 Flame.userRecord 是必填但代码传递 null。

## 发现位置
- 文件: `src/types/index.ts`
- 代码位置: 第18行、第22行

## 修复内容
修正 Flame 接口定义

## 修复方式
- **修改文件**: `src/types/index.ts`
- **修改前**:
  ```typescript
  export interface Flame {
    id: string
    title: string
    description?: string
    recommendationReason: string
    searchPhrase: string
    status: 'burning' | 'burned'
    prairieId?: string
    sourceSparkId?: string
    igniteBatchId?: string
    userRecord: string
    completedAt?: number
    createdAt: number
    isDeleted: boolean
    rekindleCount: number
    lastRekindleTime?: number
  }
  ```
- **修改后**:
  ```typescript
  export interface Flame {
    id: string
    title: string
    description?: string
    status: 'active' | 'archived'
    prairieId?: string
    sourceSparkId?: string
    igniteBatchId?: string
    userRecord?: string
    completedAt?: number
    createdAt: number
    isDeleted: boolean
    rekindleCount: number
    lastRekindleTime?: number
  }
  ```

## 验证结果
- **验证方式**: npm test
- **验证时间**: 2026-04-27
- **结果**: ✅ 通过 (78 tests passed)
- **备注**: 同时修复 Spark.sourceType 添加 'rekindle' 类型